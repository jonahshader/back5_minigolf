// Declare variables. these will be overrided by powerups that inherit from this object.
overlay_sprite = noone;							// default no overlay sprite
powerup_color = make_color_rgb(255, 255, 255);  // default white
transfer_color = false;							// if this is set to true, then the powerup's color will be applied to the ball